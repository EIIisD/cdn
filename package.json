{
  "name": "cdn",
  "type": "module",
  "scripts": {
    "build": "git add . && git commit --allow-empty-message -m '' --no-verify && git push && vc"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vercel": "^35.2.1"
  }
}